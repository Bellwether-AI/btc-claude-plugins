---
name: work-bellwether-project
description: Deprecated alias for /co-dwerker:work. Kept so the old command name still resolves.
disable-model-invocation: true
---

# Deprecated

`/co-dwerker:work-bellwether-project` was renamed to `/co-dwerker:work` in v0.2.0. Tell the user,
then invoke `co-dwerker:work`, which supports both repo mode (GitHub Issues) and project mode
(GitHub Projects board).
